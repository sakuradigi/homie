# Changelog

版本號格式：`v主版本.功能迭代.小修正`

---

## v0.8.0 — 2026-09-05

### 新增
- **補充提示欄位**：科目選擇下方可用自己的話交代需求（如「這是童詩作業，請幫忙提點」），內容會併進 prompt。科目分類套不上的作業（童詩、閱讀心得、自編講義…）特別好用
  - 最多 120 字，有清除鈕；解題結果會顯示這次套用了哪句提示
  - 刻意不存進 localStorage，避免上一份作業的提示悄悄套用到下一份；按「新題目」也會一併清除
- 解題回答改為**先給「需要訂正」摘要**（題號 → 錯在哪 → 正確答案），再逐題說明，家長不用在長篇解說裡找重點
- 歷史紀錄會一併記下當時用的補充提示
- 新增 `test_homie.mjs`（jsdom 行為測試，34 項）：改完 index.html 可先跑一次確認沒改壞

### 修正
- **OpenAI 供應商先前無法使用**：GPT-5 系列已不接受 `max_tokens`，會回 400 錯誤。改送 `max_completion_tokens`（舊的 GPT-4 系列仍走 `max_tokens`）
- **Gemini 回答可能被截斷或空白**：Gemini 3.x 的思考（thinking）token 也算在 `maxOutputTokens` 內，原本 8192 容易被吃光。上限提高到 32768（只有實際產生的 token 才計費，不會多花錢）
- 三家供應商都加上截斷偵測，回答沒寫完時會顯示提醒，建議分批拍攝
- Gemini 串流改為過濾思考內容並讀取所有 parts，避免思考過程混進答案或漏字
- `stopTTS()` 在沒有語音合成 API 的瀏覽器會出錯，連帶讓「新題目」「切換分頁」失效 → 加上防護
- 歷史紀錄預覽會出現 `<mark>` 等標記文字 → 改為先去除 HTML／markdown 記號
- 手機版輸入框字級調為 16px，避免 iOS Safari 聚焦時整頁被放大
- `.gitignore` 補上 `node_modules/`，避免測試套件被 `deploy.sh` 的 `git add .` 一起送上去

---

## v0.7.3 — 2026-09-04

### 修正
- Gemini 模型更新：3.7 Flash／3.6 Flash → **3.8 Flash（新預設）**／3.7 Flash（備用）
  - 3.8 Flash 為 Google 9/2 發布的最新 GA 版本，各項 benchmark 超越 3.7 Flash，價格不變
  - 同樣支援圖片輸入，相容現有 API 呼叫方式
- README 模型費用參考表同步更新

---

## v0.7.2 — 2026-09-01

### 修正
- Gemini 模型更新：3.6 Flash／3.5 Flash → **3.7 Flash（新預設）**／3.6 Flash（備用）
  - 3.7 Flash 為 Google 最新 GA 版本，程式碼與 agentic 任務能力更強
  - 3.5 Flash 官方建議直接遷移到 3.7，已從清單移除
  - 兩者目前皆為限時優惠價（$0.75/$3.75 每百萬 tokens，至 2026-12-31），估計費用同步調低
- README 模型費用參考表同步更新，並註記優惠價到期日

---

## v0.7.1 — 2026-07-30

### 修正
- 更新三家 AI 供應商模型清單至最新版本：
  - Gemini：3.5 Flash/Pro → **3.6 Flash（新預設）**／3.5 Flash（3.5 Pro 已下架，官方目前無穩定版 Pro 可用）
  - Claude：Sonnet 4.6 → **Sonnet 5**；Opus 4.8 → **Opus 5**（Haiku 4.5 不變）
  - OpenAI：GPT-4.1／4.1 Mini → **GPT-5.6 Sol／GPT-5.4 Mini**；新增 **GPT-5.6 Luna** 作為預設推薦
- 同步更新各模型的估計費用／次（依官方最新定價重新試算）
- README 模型費用參考表同步更新

---

## v0.7.0 — 2026-07-20

### 新增
- 解題助手：AI 會用紅色標記把孩子答錯、需要訂正的內容標出來（`<mark>` 標籤 + CSS 高亮），不用再從一堆說明文字裡自己找錯字

---

## v0.6.1 — 2026-07-06

### 修正
- 清除品牌更名（KidAI → Homie 作業通）殘留：README、about.html 的 GitHub 連結改回 `sakuradigi/homie`
- `index.html` 歷史紀錄 localStorage key 由 `kidai_*` 改為 `homie_*`，並加入舊 key 自動遷移，老用戶紀錄不受影響

---

## v0.6.0 — 2026-07-06

### 新增
- `about.html` 完整介紹／銷售頁：使用情境、畫面圖例、家長回饋（占位）、隱私、費用、FAQ
- 首頁頂部輕量介紹區：首次造訪顯示，按「開始使用」或已是老用戶（有 Key／紀錄）自動收合
- Header 新增「關於 Homie」連結

---

## v0.5.0 — 2026-07-06

### 新增
- PWA：可「加入主畫面」當 App 用，外殼離線可開（manifest.json + sw.js + 專用 icon）
- 串流回應：三家 API 全面改為 SSE 串流，解題結果逐字即時顯示，等待體感大幅縮短
- 繪本辨識過程即時預覽純文字，完成後轉為可點段朗讀版

---

## v0.4.0 — 2026-07-06

### 新增
- 圖片上傳前自動壓縮（長邊 1600px、JPEG 0.85）：費用大幅降低、上傳更快
- API 錯誤訊息友善化（429 顯示「請求太頻繁」）
- DOMPurify 淨化 AI 回應後再渲染（防 XSS）

### 修正
- 點擊歷史紀錄恢復時不再重複寫入歷史（原本每點一次多存一筆）
- 朗讀語速／語音選單改為單一全域控制（原本僅第一頁的選單有效，且產生重複 id）
- 英文繪本歷史紀錄恢復後維持英文朗讀（原本一律變中文）
- 繪本分頁辨識容忍空格與英文變體（`=== 第 1 頁 ===`、`=== Page 1 ===`）
- 語音清單改用 `voiceschanged` 事件載入（原本賭 300ms 時序）
- 空白 API Key 不再顯示「已儲存」
- 切換分頁時自動停止朗讀
- Gemini Key 改走 header 不再出現在 URL；max_tokens 提高至 8192 避免多頁截斷

### 移除
- 半套的英文介面切換（多處文案未翻譯，維護成本大於價值）
- 死碼：`escapeHtml()`、`updateSpeed()`、`buildTTSControls()`

---

## v0.3.1 — 2026-06-08

### 更新
- 品牌更名：KidAI → **Homie 作業通**
- 新增 `deploy.sh` 一鍵部署腳本
- README 完整改寫

---

## v0.3.0 — 2026-06-07

### 新增
- 歷史紀錄：自動儲存最近10筆結果，可點擊恢復（localStorage）
- 「新題目」一鍵清除照片與結果，重新開始
- 連續拍攝提示列：拍完一張立即顯示「再拍一張」

### 修正
- Markdown 符號（`###`、`**`）不再以原始文字顯示，改為正確渲染
- 相機按鈕第二次點擊無反應的 bug（iOS Safari input value reset）
- 相機改為 button + 隱藏 input，iOS 相容性更佳

---

## v0.2.0 — 2026-06-07

### 新增
- 模型選擇下拉（Gemini 2.0 Flash / 3.5 Flash / 2.5 Flash / 2.5 Pro）
- 每次請求估計費用顯示
- RWD 手機版優化：觸控目標放大、主按鈕固定底部、iPhone 安全區域

### 修正
- 上傳按鈕拆分為「📷 拍照」+「🖼️ 從相簿選」

---

## v0.1.0 — 2026-06-07

### 初始版本
- 解題助手：拍照 → Gemini Vision → 逐題回答
- 繪本朗讀：拍照 → OCR 去雜訊 → 分段朗讀
- Gemini API Key 自填（不經過本站）
- 繁中 / English 雙語介面切換
